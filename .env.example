APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost
ASSET_URL={$APP_URL}
APP_TIMEZONE=Asia/Jakarta
APP_LOCALE=id
APP_FALLBACK_LOCALE=id
APP_FAKER_LOCALE=id_ID

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=laravel
DB_USERNAME=root
DB_PASSWORD=

BROADCAST_CONNECTION=null
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

MINIO_ACCESS_KEY_ID=
MINIO_SECRET_ACCESS_KEY=
MINIO_DEFAULT_REGION=us-east-1
MINIO_BUCKET=
MINIO_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

# WhatsApp gateway — WAHA (primary, self-hosted) dengan Fonnte sebagai cadangan
WHATSAPP_GATEWAY_PROVIDER=waha
WHATSAPP_GATEWAY_FALLBACK_PROVIDER=fonnte
WHATSAPP_GATEWAY_FALLBACK_ENABLED=true

WHATSAPP_GATEWAY_WAHA_BASE_URL=http://localhost:3000
WHATSAPP_GATEWAY_WAHA_API_KEY=
WHATSAPP_GATEWAY_WAHA_SESSION=default

WHATSAPP_GATEWAY_FONNTE_ENDPOINT=https://api.fonnte.com/send
WHATSAPP_GATEWAY_FONNTE_TOKEN=

WHATSAPP_GATEWAY_COUNTRY_CODE=62
WHATSAPP_GATEWAY_DEFAULT_TARGET=
WHATSAPP_GATEWAY_TIMEOUT=15
WHATSAPP_GATEWAY_MIN_SECONDS_BETWEEN_SENDS=3
WHATSAPP_GATEWAY_MIN_DIGITS=10
WHATSAPP_GATEWAY_MAX_DIGITS=15
