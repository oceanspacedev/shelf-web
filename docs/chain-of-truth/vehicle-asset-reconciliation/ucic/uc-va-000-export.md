# API-VA-000 — Export Contract

Status: Reviewed

Use case: UC-VA-000.

Interface: `VehicleAssetAuditWorkbookExporter::exportToPath(): string`.

Input: tidak ada (membaca Asset MOBIL/MOTOR). Output: path file `.xlsx` sheet `Monitoring Asset` + `PETUNJUK`. Side effect: file temp saja.
